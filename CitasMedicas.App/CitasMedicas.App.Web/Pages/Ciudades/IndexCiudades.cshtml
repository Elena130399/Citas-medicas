@page
@model CitasMedicas.App.Web.Pages.IndexCiudadesModel
@{
}
<h1>Listado de Ciudades</h1>


<table class="table table-striped">
    <thead>
        <tr>
            <th scope="col">Nombre</th>
            <th colspan=2 scope="col">Acciones</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var ciudad in Model.ciudades)
        {
            <tr>
                <td>@ciudad.NombreCiudad</td>
                
                <td><a class="btn btn-secondary" asp-page="./EditCiudad" asp-route-id="@ciudad.Id">Editar</a></td>
                <td><a class="btn btn-secondary" asp-page="./DeleteCiudad" asp-route-id="@ciudad.Id">Eliminar</a></td>
                
            </tr>
        }
    </tbody>


</table>